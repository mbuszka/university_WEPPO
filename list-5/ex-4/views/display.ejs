<hml>
  <head>
    <style>
      table, th, td {
        border: 1px solid black;
        border-collapse: collapse;
      }

      th, td {
        padding: 15px;
      }
    </style>
  </head>
  <body>
    <table>
      <tr>
        <td>Imię</td>
        <td><%= name %></td>
        <td>Nazwisko</td>
        <td><%= surname %></td>
      </tr>
      <tr>
        <td>Przedmiot</td>
        <td><%= class_ %></td>
        <td>Data</td>
        <td><%= date %></td>
      </tr>
    </table>
    <table>
      <tr>
        <% for (var i = 0; i < 10; i++) { %>
          <td><%= i %></td>
        <% } %>
      </tr>
      <tr>
        <% for (var i = 0; i < 10; i++) { %>
          <td><%= exercises[i] %></td>
        <% } %>
      </tr>
    </table>
  </body>
</hml>
